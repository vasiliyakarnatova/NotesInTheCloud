{
  "name": "notes-in-the-cloud",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && npm --prefix web install && npm --prefix web/frontend/main install",
    "devBackend": "npm --prefix web run devBackend",
    "devAuth": "npm --prefix web run devAuth",
    "devMain": "npm --prefix web/frontend/main run dev",
    "dev": "concurrently -n backend,auth,main -c blue,magenta,green \"npm run devBackend\" \"npm run devAuth\" \"npm run devMain\"",
    "down": "npm --prefix web run down",
    "migrate": "npm --prefix web run migrate",
    "docker-container": "--prefix web docker-compose up -d"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}